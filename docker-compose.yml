services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"

  frontend-react:
    build: ./frontend-react
    ports:
      - "5173:5173"
    depends_on:
      - backend
    environment:
      - VITE_API_BASE_URL=http://localhost:8000
